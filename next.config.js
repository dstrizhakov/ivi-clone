/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lh3.googleusercontent.com', 'sun6-22.userapi.com', 'vk.com'],
  },
};

module.exports = nextConfig;
